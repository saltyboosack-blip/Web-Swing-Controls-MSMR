# Third-party notices

This add-on is derived from Tkachov's GPL-licensed Overstrike/Modding Tool
source. The complete GPL v3 text is in `LICENSE.txt`.

The single-file application retains upstream dependencies:

- Windows API Code Pack 1.1 (`WindowsAPICodePack-Core` and
  `WindowsAPICodePack-Shell`), Aybe's maintained package;
- Ionic.Zlib.Core 1.0.0;
- K4os.Compression.LZ4 1.3.5, by Milosz Krajewski;
- Newtonsoft.Json 13.0.3, by James Newton-King, MIT;
- NVIDIA/libdeflate GDeflate build, distributed as `libdeflate.dll` by the
  upstream Modding Tool release.

The embedded MSMR script proxy uses MinHook and termcolor. Their license texts
and full corresponding proxy source are provided in the Web Swing Controls
repository under `ScriptsProxy/` and its release source package.

